# 14 — Long-N denseness soak (N=100)

Extension of example 10 to **N=100** for multi-round boundary / quota health
under poison+heal cadence. Pure Aura; core escapes = 0 on PASS.

## Run

```bash
./scripts/run-aura.sh examples/14-long-n-100/main.aura
```

Expected: `PASS: long-N denseness soak (N=100, …)` and
`RESULT pass example=14-long-n-100 N=100 escapes=0 …`
